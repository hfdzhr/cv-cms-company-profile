# Port & Host configuration
PORT=3000
HOST=0.0.0.0

# Node Environment ('development' or 'production')
NODE_ENV=production

# File polling for Windows host file change detection in dev (optional: set 'true' if HMR doesn't trigger)
WATCHPACK_POLLING=false

# Nuxt Public Site URL (optional override)
# NUXT_PUBLIC_SITE_URL=https://cipta-sampling.vercel.app
